name: GOOL BOT PREMATCH Scanner (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "GitHub Actions scheduler is disabled; bot runs on external hosting."
